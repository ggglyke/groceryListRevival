# .env file - Frontend Configuration

# URL publique de l'application
REACT_APP_PUBLIC_URL=https://www.guillaumejarry.com/groceryListRevival

# Nom de l'application
REACT_APP_APP_NAME=Grocery List App

# Basename pour le Router React
# En dev (npm start) : utiliser le chemin où tu accèdes à l'app en local
# En prod (npm run build) : laisser VIDE pour des chemins relatifs
# Exemple dev : REACT_APP_ROUTER_BASENAME=/groceryList
# Exemple prod : REACT_APP_ROUTER_BASENAME=
REACT_APP_ROUTER_BASENAME=/groceryList

# (OPTIONNEL) Forcer un environnement spécifique
# Par défaut, l'environnement est détecté automatiquement :
# - npm start → development (localhost)
# - npm run build → production (URL de prod)
#
# Décommenter pour forcer manuellement :
# REACT_APP_ENV=development  # Force dev même pour le build
# REACT_APP_ENV=production   # Force prod même en dev
