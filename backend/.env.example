# Base de données MongoDB
# IMPORTANT: Remplacez par votre propre URI MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/groceryList?retryWrites=true&w=majority

# JWT Secret - Utilisez une clé forte et unique en production
# Générez-en un nouveau avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CSRF Secret - Utilisez une clé forte et unique en production
CSRF_SECRET=your-super-secret-csrf-key-change-this-in-production

# Cookie Secret
COOKIE_SECRET=your-cookie-secret-change-this-in-production

# Environnement (development | production)
# IMPORTANT: En production, les cookies utilisent sameSite=none et secure=true
# pour permettre les requêtes cross-domain (frontend sur guillaumejarry.com, backend sur render.com)
NODE_ENV=development

# Cookie sécurisé - DOIT être "true" en production avec HTTPS
# Note: En production (NODE_ENV=production), secure est automatiquement true
COOKIE_SECURE=false
